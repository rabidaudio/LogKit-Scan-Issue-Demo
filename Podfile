# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LogKit-Scan-Issue-Demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'LogKit', git: 'https://github.com/glentregoning/logkit', branch: 'swift3'

  # Pods for LogKit-Scan-Issue-Demo

  target 'LogKit-Scan-Issue-DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
